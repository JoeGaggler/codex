researching migration from [[azure pipelines]] to [[github actions]], found out that i can sync repo across both by adding an additional url to **same** remote:
```
git remote set-url --add origin git@github.com:my_account/my_repo.git
```
