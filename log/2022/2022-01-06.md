tried to debug a `Publisher` ([[swift combine framework]]) i created.

it's not sending when i'd expect, and concerned about retain cycles.

temporarily implemented manually until i can revisit this.
