# generic protocols

it does not seem like a type can explicitly implement a generic protocol (i.e. has associated types) multiple times, unlike in C#

the associated types are either inferred, or are explicitly defined via `typealias` when inference is not possible.

